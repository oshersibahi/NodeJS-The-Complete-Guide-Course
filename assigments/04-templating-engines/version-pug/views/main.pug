extends layouts/main-layout.pug

block title
    title #{ pageTitle }

block styles
    link(rel="stylesheet", href="/css/users.css")

block content
    if users.length > 0
        ul
        each user in users
            li user
    else 
        h1 Users Not Found!
