<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        block title
        link(rel="stylesheet" href="/css/main.css")
        block styles


    body
        header.main-header
            .main-header-nav
                ul.main-header__item-list
                    li.main-header__item
                        a(href="/" class=(path === '/' ? 'active' : '')) Main
                    li.main-header__item
                        a(href="/users/add-user" class=(path === '/users/add-user' ? 'active' : '')) Add Users
        main
            block content